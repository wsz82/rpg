package model.item;

import model.asset.Asset;
import model.stage.Coordinates;

public class Landscape extends Item {

    public Landscape(Asset asset, Coordinates pos, int level) {
        super(asset, pos, level);
    }
}
